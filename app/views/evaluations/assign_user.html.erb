<div class="container">
  <% if @evaluation.status == 'closed' %>
      <h1>This Evaluation has already been assigned</h1>
      This Evaluation of <%= @evaluation.restaurant.name %>, is already been assigned
      to <%= @evaluation.evaluation_applications[0].user.email %>
  <% else %>
      <% if @application %>
          <h1>Evaluation Assigned</h1>
          This evaluation of <%= @evaluation.restaurant.name %> is now assigned
          to <%= @evaluation.evaluation_applications[0].user.email %>
      <% else %>
          <h1>Evaluator Hasn't Applied</h1>
          <%= @applicant.email %> has not applied to this evaluation of <%= @evaluation.restaurant.name %>
      <% end %>
  <% end %>
</div>